---
import type { CollectionEntry } from 'astro:content';
import type { Locale } from '../lib/i18n';

interface Props {
  locale: Locale;
  title: string;
  entries: CollectionEntry<'certifications'>[];
  hidden?: boolean;
}

const { locale, title, entries, hidden } = Astro.props;
---

{entries.length > 0 && (
  <section data-locale={locale} class="py-section" hidden={hidden || undefined}>
    <div class="mx-auto max-w-3xl px-6">
      <h2
        class="text-3xl font-semibold tracking-[-0.025em] text-ink sm:text-4xl"
        data-reveal
      >
        {title}
      </h2>
      <ul class="mt-10 space-y-4">
        {
          entries.map((entry) => (
            <li
              class="flex items-start justify-between gap-4 rounded-lg border border-hairline bg-surface-1 p-5 transition-colors hover:bg-surface-2"
              data-reveal
            >
              <div>
                <h3 class="text-base font-medium text-ink">{entry.data.title}</h3>
                <p class="mt-1 text-sm text-ink-muted">{entry.data.issuer}</p>
              </div>
              <div class="text-right">
                <p class="text-[12px] text-ink-subtle">{entry.data.date}</p>
                {entry.data.url && (
                  <a
                    href={entry.data.url}
                    target="_blank"
                    rel="noopener noreferrer"
                    class="mt-1 inline-flex items-center gap-1 text-sm text-ink-subtle transition-colors hover:text-primary"
                  >
                    <svg
                      class="h-3.5 w-3.5"
                      viewBox="0 0 24 24"
                      fill="none"
                      stroke="currentColor"
                      stroke-width="2"
                      stroke-linecap="round"
                      stroke-linejoin="round"
                      aria-hidden="true"
                    >
                      <path d="M7 17L17 7M9 7h8v8"></path>
                    </svg>
                  </a>
                )}
              </div>
            </li>
          ))
        }
      </ul>
    </div>
  </section>
)}
