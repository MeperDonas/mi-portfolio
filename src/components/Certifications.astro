---
import type { CollectionEntry } from 'astro:content';
import type { Locale } from '../lib/i18n';

interface Props {
  locale: Locale;
  title: string;
  entries: CollectionEntry<'certifications'>[];
  hidden?: boolean;
}

const { locale, title, entries, hidden } = Astro.props;
---

{entries.length > 0 && (
  <section data-locale={locale} class="py-section" hidden={hidden || undefined}>
    <div class="px-6">
      <h2
        class="text-3xl font-semibold tracking-tight[-0.025em] text-ink sm:text-4xl"
        data-reveal
      >
        {title}
      </h2>
      <ul class="mt-10 grid gap-6 sm:grid-cols-2">
        {
          entries.map((entry) => (
            <li
              class="flex flex-col rounded-lg border border-hairline bg-surface-1 p-6 transition-colors hover:bg-surface-2"
              data-reveal
            >
              <h3 class="text-lg font-medium text-ink">{entry.data.title}</h3>
              <p class="mt-1 text-sm text-ink-muted">{entry.data.issuer}</p>
              <div class="mt-auto flex items-center justify-between pt-6">
                <p class="text-[12px] text-ink-subtle">{entry.data.date}</p>
                {entry.data.url && (
                  <a
                    href={entry.data.url}
                    target="_blank"
                    rel="noopener noreferrer"
                    class="text-ink-subtle transition-colors hover:text-primary"
                  >
                    <svg
                      class="h-3.5 w-3.5"
                      viewBox="0 0 24 24"
                      fill="none"
                      stroke="currentColor"
                      stroke-width="2"
                      stroke-linecap="round"
                      stroke-linejoin="round"
                      aria-hidden="true"
                    >
                      <path d="M7 17L17 7M9 7h8v8"></path>
                    </svg>
                  </a>
                )}
              </div>
            </li>
          ))
        }
      </ul>
    </div>
  </section>
)}
